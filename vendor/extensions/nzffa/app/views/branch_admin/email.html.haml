%h2 Email #{@group.name}#{' past' if @past_members}#{' last year '} members

%p Copy and Paste the text below into the BCC field of a new email

%p=@readers.map{|r| "\"#{r.name}\" &lt;#{r.email}&gt;"}.join(', ')

