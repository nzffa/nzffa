%p Dear #{@advert.reader.forename} #{@advert.reader.surname},

%p Your listing titled '#{@advert.title}' is due to expire on #{l @advert.expires_on}.

-hostname = ActionMailer::Base.default_url_options[:host]
-my_adverts_url = "http://#{hostname}"+MarketplaceController::MY_ADVERTS_PATH

%p To extend the expiry date by one month, visit #{link_to(my_adverts_url, my_adverts_url)} and click Extend Expiry.

%p Thank you.
