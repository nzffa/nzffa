%p Dear #{reader.forename} #{reader.surname},

%p Your listing titled '#{advert.title}' is due to expire on #{advert.expires_on}.

%p To extend the expiry date by one month, visit #{link_to(AdvertsController::MY_ADVERTS_URL, AdvertsController::MY_ADVERTS_URL)} and click Extend Expiry.

%p Thank you.
