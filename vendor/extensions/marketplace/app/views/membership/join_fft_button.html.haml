-if @reader
  -if @reader.group_ids.include?(MembershipController::FFT_GROUP_ID)
    =link_to('You are a member of the FFT Group. Go to the Membership area', MembershipController::FFT_MEMBERS_AREA_PATH)
  -else
    =link_to('Join Farm Foresty Timbers so you can place adverts.', join_fft_path)
-else
  You need to #{link_to('register', register_path)} or #{link_to('login', reader_login_path)} before you can join the marketplace
