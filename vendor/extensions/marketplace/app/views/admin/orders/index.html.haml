.outset
  = render :partial => 'index_table', :locals => {:orders => @orders}

  #actions
    = pagination_for(@orders)
    %ul
      %li= link_to image('plus') + " new order", new_admin_order_url, :class => 'action_button'



