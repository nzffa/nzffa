= render :partial => 'print_css'

-@unsaved_orders.each do |order|
  -@order = order
  -@subscription = order.subscription
  -@reader = @subscription.reader
  =render :partial => 'print'
  .page-break
