%h1 Print Subscription Renewal Forms

#{@subscriptions_count} subscriptions in #{@num_batches} batches of #{@batch_size} subscriptions each

%ul
-@num_batches.times do |batch|
  - last_batch ||= 0
  %li=link_to "batch #{batch}", print_batch_admin_subscriptions_path(@options.merge({:batch => batch}))
  - last_batch = batch

