%h1 Adverts
%table
  %thead
    %th Owner
    %th Title
    %th Category
    %th Snippet
    %th Expires On
    %th edit
    %th destroy
  %tbody
    -@adverts.each do |advert|
      %tr
        %td=advert.reader.name
        %td=advert.title
        %td=advert.categories.join(', ')
        %td=advert.snippet
        %td=advert.expires_on
        %td=link_to 'edit', [:edit, :admin, advert]
        %td=link_to 'destroy', [:admin, advert], :method => :delete, :confirm => "Really delete advert from '#{advert.reader.organisation}'?"
